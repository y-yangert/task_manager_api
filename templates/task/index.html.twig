{# templates/task/index.html.twig #}
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Task Manager</title>
    <style>
        body { font-family: -apple-system, BlinkMacSystemFont, sans-serif; max-width: 800px; margin: 0 auto; padding: 20px; }
        table { width: 100%; border-collapse: collapse; margin: 20px 0; }
        th, td { padding: 12px; text-align: left; border-bottom: 1px solid #eee; }
        th { background: #f8f9fa; font-weight: 600; }
        .completed { opacity: 0.6; }
        .btn { padding: 8px 16px; border: none; border-radius: 6px; cursor: pointer; text-decoration: none; display: inline-block; margin: 2px; }
        .btn-primary { background: #007bff; color: white; }
        .btn-success { background: #28a745; color: white; }
        .btn-warning { background: #ffc107; color: #212529; }
        .btn-danger { background: #dc3545; color: white; }
        .no-tasks { text-align: center; color: #666; padding: 40px; }
    </style>
</head>
<body>
    <h1>📋 Task Manager</h1>
    
    <div style="margin-bottom: 20px;">
        <a href="{{ path('app_task_new') }}" class="btn btn-primary">New Task</a>
    </div>

    {% if tasks|length > 0 %}
        <table>
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Title</th>
                    <th>Due Date</th>
                    <th>Status</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for task in tasks %}
                    <tr class="{% if task.isCompleted %}completed{% endif %}">
                        <td>{{ task.id }}</td>
                        <td>{{ task.title }}</td>
                        <td>{{ task.dueDate ? task.dueDate|date('M d, Y') : 'No date' }}</td>
                        <td>
                            {% if task.isCompleted %}
                                <span style="color: #28a745;">✅ Completed</span>
                            {% else %}
                                <span style="color: #ffc107;">⏳ Pending</span>
                            {% endif %}
                        </td>
                        <td>
                            <a href="{{ path('app_task_edit', {'id': task.id}) }}" class="btn btn-warning">✏️ Edit</a>
                            <a href="{{ path('app_task_delete', {'id': task.id}) }}" class="btn btn-danger" 
                               onclick="return confirm('Delete this task?')">🗑️ Delete</a>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    {% else %}
        <div class="no-tasks">
            <h3>No tasks yet</h3>
            <p>Create your first task above!</p>
            <a href="{{ path('app_task_new') }}" class="btn btn-primary">➕ Add First Task</a>
        </div>
    {% endif %}

    <div style="margin-top: 40px; font-size: 14px; color: #666;">
        <p><strong>API:</strong> <a href="/api/docs">Swagger Docs</a> | 
           <a href="/api/tasks">/api/tasks</a></p>
    </div>
</body>
</html>